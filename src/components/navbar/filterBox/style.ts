import styled from "styled-components";

export const StyledOptions = styled.li`
  color: var(--grey3);
  list-style: none;

  cursor: pointer;

  :hover {
    transition: 0ms.5;
    color: var(--brand1);
  }
`;
